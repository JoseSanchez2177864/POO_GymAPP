<Configp>:
    BoxLayout:
        orientation: 'vertical'
        padding: 10
        spacing: 10

        # Encabezado
        BoxLayout:
            size_hint_y: None
            height: 60
            canvas.before:
                Color:
                    rgba: 0.9, 0.9, 0.9, 1
                Rectangle:
                    pos: self.pos
                    size: self.size

            Label:
                text: "[b]CONFIGURACIÓN DE USUARIO[/b]"
                markup: True
                font_size: 24

        # Información del usuario
        BoxLayout:
            orientation: 'vertical'
            padding: 10
            spacing: 10

            Label:
                text: "Nombre: [b]{} {}[/b]".format(root.nombre, root.apellidos)
                markup: True
                font_size: 18

            Label:
                text: "Nombre de Usuario: [b]{}[/b]".format(root.nombreusuario)
                markup: True
                font_size: 18
            
            Label:
                text: "Correo: [b]{}[/b]".format(root.correo)
                markup: True
                font_size: 18

        # Opciones de configuración
        BoxLayout:
            orientation: 'vertical'
            padding: 10
            spacing: 10

            Button:
                text: "Cambiar contraseña"
                size_hint_y: None
                height: 50
                font_size: 18
                on_release: root.mostrar_popup_contrasena()

            Button:
                text: "Actualizar información"
                size_hint_y: None
                height: 50
                font_size: 18
                on_release: root.mostrar_popup_info()

            Button:
                text: "Cerrar sesión"
                size_hint_y: None
                height: 50
                font_size: 18
                background_color: 0.8, 0.1, 0.1, 1
                color: 1, 1, 1, 1
                on_release:
                    app.root.current = 'pantalla1'
        BoxLayout:
            size_hint_y: None
            height: 60
            spacing: 10

            Button:
                text: "🏠"
                on_release:
                    app.root.current = 'pantalla3'
            Button:
                text: "➕"
            Button:
                text: "📊"
                on_release:
                    app.root.current = 'pantalla7'
                opacity: 1 if root.rol == 1 else 0
                disabled: root.rol != 1
            Button:
                text: "👤"
                on_release:
                    app.root.current = 'pantalla5'