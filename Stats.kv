<Statsp>:
    BoxLayout:
        orientation: 'vertical'
        padding: 10
        spacing: 10

        Label:
            text: "Progreso de RM"
            font_size: 24

        Image:
            id: imagen_musculos
            source: 'musculos_base.png'  # Imagen base de músculos
            size_hint_y: 0.5

        BoxLayout:
            size_hint_y: 0.5
            Image:
                id: grafica_rm
                source: 'grafica_rm.png'
    BoxLayout:
        size_hint_y: None
        height: 60
        spacing: 10
        Button:
            text: "🏠"
            on_release:
                app.root.current = 'pantalla3'
        Button:
            text: "➕"
            on_release:app.redirigir_por_rol()
        Button:
            text: "STATS"
            on_release:app.redirigir_por_rol2()
        Button:
            text: "📊"
            on_release:
                app.root.current = 'pantalla7'
            opacity: 1 if root.rol == 1 else 0
            disabled: root.rol != 1
        Button:
            text: "👤"
            on_release:
                app.root.current = 'pantalla5'
